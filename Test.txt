Map<String, List<ApplicationUser>> sortedGroupUserMapping = new LinkedHashMap<>();

groupUserMapping.entrySet().stream()
    .sorted(Comparator.comparingInt(entry -> extractKmNumber(entry.getKey())))
    .forEachOrdered(entry -> sortedGroupUserMapping.put(entry.getKey(), entry.getValue()));

// Alte Map durch sortierte ersetzen
groupUserMapping = sortedGroupUserMapping;
