import com.atlassian.jira.component.ComponentAccessor
import com.atlassian.jira.config.util.JiraHome

def jiraHome  = ComponentAccessor.getComponent(JiraHome).home          // …/data/jira
def TARGET_DIR = new File("${jiraHome}/export/archive")                // immer vorhanden
TARGET_DIR.mkdirs()                                                    // erzeugt den Ordner rekursiv
