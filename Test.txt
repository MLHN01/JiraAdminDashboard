import com.atlassian.jira.plugin.issueview.IssueViewFieldParamsImpl
import com.atlassian.jira.plugin.issueview.IssueViewRequestParamsImpl

// leere Field-Parameter – wir brauchen nur das Standard-XML
def fieldParams = new IssueViewFieldParamsImpl(
        false,           // customViewRequested
        []  as Set,      // fieldIds
        []  as Set,      // orderableFieldIds
        []  as Set,      // customFieldIds
        false            // allCustomFields
)

// damit erstellt du das Request-Objekt
def params = new IssueViewRequestParamsImpl(fieldParams)
